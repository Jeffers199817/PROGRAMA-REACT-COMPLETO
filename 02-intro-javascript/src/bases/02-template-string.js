const nombre = "Jefferson";
const apellido = "Aluqinga";

//const nombresCompletos = nombre + " " + apellido;

//const nombreCompleto = `${nombre} ${apellido}`;

function getSaludo(nombre, apellido) {
  return `Hola mundo desde ${nombre} ${apellido}`;
}

console.log(getSaludo(nombre, apellido));
